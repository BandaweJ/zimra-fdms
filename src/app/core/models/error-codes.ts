// Placeholder error codes.
// Later: expand to exact DEV/RCPT/FISC/FILE codes from spec.

export enum DevErrorCode {
  Unknown = 'DEV_UNKNOWN',
}

export enum ReceiptErrorCode {
  Unknown = 'RCPT_UNKNOWN',
}

export enum FiscalErrorCode {
  Unknown = 'FISC_UNKNOWN',
}

export enum FileErrorCode {
  Unknown = 'FILE_UNKNOWN',
}

